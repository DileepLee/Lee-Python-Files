def add(a,b): #-->This is the non perameters, Because we should not pass the value here so this is non perameters...
    c = a+b
    print(c)

#---> Compalsary we should pass the value here we did not pass..it will gives Error Message.